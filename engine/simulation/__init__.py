"""Chapter 6.4 Routing Simulation Model, re-scoped (DDE-036).

The RSM is retained and repositioned: an evaluation and
fixture-generation subsystem, never a training source for a production
policy and never an authority (Chapter 6.4). See
`engine.simulation.scenarios` for the real, deterministic adversarial
fixture generator and `engine.simulation.service` for the durable,
reproducibility-preserving writer of `routing_simulation_runs`.
"""

from __future__ import annotations
