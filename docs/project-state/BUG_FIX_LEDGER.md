# DDE Fixed-Bug Ledger

> Generated from `BUG_FIX_LEDGER.jsonl`. Do not edit this file directly.
> The JSONL ledger is append-only; corrections are new entries, never history rewrites.

| Bug | Fixed | Severity | Summary | Fix commit |
| --- | --- | --- | --- | --- |
| `DDE-BUG-0001` | 2026-09-10 | HIGH | Windows Studio client tests depended on shell glob expansion | `262c712ef988fcbad7d67cc294205afec6f636d2` |
| `DDE-BUG-0002` | 2026-09-10 | HIGH | Playwright workbench server failed to become ready in CI | `262c712ef988fcbad7d67cc294205afec6f636d2` |
| `DDE-BUG-0003` | 2026-09-10 | HIGH | VEKL activation manifest ORM omitted knowledge_context | `262c712ef988fcbad7d67cc294205afec6f636d2` |
| `DDE-BUG-0004` | 2026-09-10 | HIGH | VEKL JSONB persistence attempted to serialize raw UUID objects | `1c1b49a6c35998d75e6f1c44e3d278f88aecf9c0` |
| `DDE-BUG-0005` | 2026-09-10 | MEDIUM | Truth-change invalidation emitted a non-canonical reason code | `2e532dbfc52b2025da7646d99aee299b9eb0a6dc` |
| `DDE-BUG-0006` | 2026-09-10 | CRITICAL | GraphRAG resolution trace invalidated itself on Unit readiness promotion | `5f09c5d7195aee7112b5b0f473e860b41a1a9d20` |
| `DDE-BUG-0007` | 2026-09-10 | CRITICAL | Unit-to-TaskGraph edge provenance still leaked mutable Unit-map state | `d153122360f6336acd3259c3ec3205fd305c78ab` |
| `DDE-BUG-0008` | 2026-09-10 | HIGH | Project Truth autolog attempted forbidden writes to protected main | `d66a2090bf8d73fd6d192d438a4dea745a8e86c8` |

## DDE-BUG-0001 — Windows Studio client tests depended on shell glob expansion

**Symptom:** The Windows DDE Studio compile job could not execute the compiled shared test set even though the tests were valid.

**Root cause:** The npm test path relied on a POSIX-style out/shared/*.test.js shell glob that Windows did not expand.

**Resolution:** Added a cross-platform compiled-test launcher so the same test inventory executes on Windows and POSIX hosts.

**Affected scope:** `interfaces/dde-studio`, `GitHub Actions DDE Studio compile`

**Regression proof:** `DDE Studio Windows client suite 77/77`

**Evidence:** `github-actions:DDE-Studio certified run 34491084691`

**Fix:** `262c712ef988fcbad7d67cc294205afec6f636d2` — fix(ci): harden VEKL and Studio certification seams

## DDE-BUG-0002 — Playwright workbench server failed to become ready in CI

**Symptom:** The Linux workbench browser job timed out before structural tests could run.

**Root cause:** The Playwright web-server command depended on indirect Vite startup/binding behavior that did not reliably become reachable in CI.

**Resolution:** Bound the local Vite executable explicitly to 127.0.0.1 so the Playwright readiness probe reaches the exact server under test.

**Affected scope:** `interfaces/dde-studio/ui`, `GitHub Actions DDE Studio workbench`

**Regression proof:** `DDE Studio Knowledge workbench browser suite 80/80`

**Evidence:** `github-actions:DDE-Studio certified run 34491084691`

**Fix:** `262c712ef988fcbad7d67cc294205afec6f636d2` — fix(ci): harden VEKL and Studio certification seams

## DDE-BUG-0003 — VEKL activation manifest ORM omitted knowledge_context

**Symptom:** Service-backed VEKL tests failed after migration because runtime inserts referenced a schema field absent from SQLAlchemy table metadata.

**Root cause:** Migration, JSON schema, and generated contract added knowledge_context but engine/vekl/tables.py was not updated in the same change.

**Resolution:** Added knowledge_context to the SQLAlchemy activation-manifest table and protected the mapping with regression coverage.

**Affected scope:** `engine/vekl/tables.py`, `vekl_activation_manifests`

**Regression proof:** `affected VEKL regression suite 63/63`, `PostgreSQL VEKL integration suite`

**Evidence:** `github-actions:core certified run 34497981504`

**Fix:** `262c712ef988fcbad7d67cc294205afec6f636d2` — fix(ci): harden VEKL and Studio certification seams

## DDE-BUG-0004 — VEKL JSONB persistence attempted to serialize raw UUID objects

**Symptom:** PostgreSQL integration failed while persisting Unit knowledge structures containing UUID-valued JSONB references.

**Root cause:** Knowledge repository writes used raw model_dump values for JSONB payloads, leaving UUID objects for the JSON encoder.

**Resolution:** Normalized nested JSONB values to JSON-safe representations at the repository boundary while retaining native UUIDs in typed PostgreSQL UUID columns.

**Affected scope:** `engine/vekl/knowledge_repository.py`, `VEKL JSONB persistence`

**Regression proof:** `service-free JSONB UUID normalization regression`, `PostgreSQL VEKL integration suite`

**Evidence:** `github-actions:core certified run 34497981504`

**Fix:** `1c1b49a6c35998d75e6f1c44e3d278f88aecf9c0` — fix(vekl): normalize knowledge persistence and browser assertion

## DDE-BUG-0005 — Truth-change invalidation emitted a non-canonical reason code

**Symptom:** Accepted Truth Challenge invalidation failed contract validation on the Project Truth change path.

**Root cause:** One runtime path emitted PROJECT_TRUTH_CHANGED while the generated contract permits VEKL_PROJECT_TRUTH_CHANGED.

**Resolution:** Aligned the runtime invalidation path to the canonical VEKL_PROJECT_TRUTH_CHANGED enum and retained strict contract validation.

**Affected scope:** `engine/vekl/knowledge_service.py`, `VEKL graph invalidation`

**Regression proof:** `focused VEKL/Gateway suite 22/22`, `PostgreSQL Truth Challenge integration`

**Evidence:** `github-actions:core certified run 34497981504`

**Fix:** `2e532dbfc52b2025da7646d99aee299b9eb0a6dc` — fix(vekl): close graph selection and invalidation contracts

## DDE-BUG-0006 — GraphRAG resolution trace invalidated itself on Unit readiness promotion

**Symptom:** A successful MAPPING-to-READY transition changed the graph snapshot immediately after resolution, making the just-created trace stale.

**Root cause:** The Unit graph node identity incorporated mutable unit_map_hash/readiness state instead of immutable structural Unit revision identity.

**Resolution:** Changed Unit graph identity to unit_revision_hash so readiness transitions cannot alter the structural GraphRAG snapshot.

**Affected scope:** `engine/vekl/knowledge_service.py`, `VEKL graph snapshot determinism`

**Regression proof:** `focused VEKL/Gateway suite 22/22`, `PostgreSQL execution-binding integration`

**Evidence:** `github-actions:core certified run 34497981504`

**Fix:** `5f09c5d7195aee7112b5b0f473e860b41a1a9d20` — fix(vekl): keep graph snapshots stable across readiness

## DDE-BUG-0007 — Unit-to-TaskGraph edge provenance still leaked mutable Unit-map state

**Symptom:** After the Unit-node fix, one PostgreSQL integration still observed graph snapshot churn across readiness promotion.

**Root cause:** The Unit-to-TaskGraph edge provenance continued to use mutable unit_map_hash even though the Unit node had moved to unit_revision_hash.

**Resolution:** Bound the edge provenance to immutable unit_revision_hash and audited the compiler for remaining readiness/challenge/map-hash leakage.

**Affected scope:** `engine/vekl/knowledge_service.py`, `VEKL graph edge provenance`

**Regression proof:** `focused VEKL/Gateway suite 22/22`, `PostgreSQL VEKL integration 8/8`

**Evidence:** `github-actions:core run 34495889745`, `github-actions:core certified run 34497981504`

**Fix:** `d153122360f6336acd3259c3ec3205fd305c78ab` — fix(vekl): stabilize unit graph edge provenance

## DDE-BUG-0008 — Project Truth autolog attempted forbidden writes to protected main

**Symptom:** After protected merge, the Project Truth workflow failed because it tried to create and push a follow-up ledger commit directly to protected main.

**Root cause:** The original autolog design treated CI as a writer even though branch protection correctly requires main changes to arrive through protected PRs.

**Resolution:** Converted Project Truth automation into a read-only guard, added durable API-backfill handling and pure-merge-carrier semantics, and retained pre-merge staged-diff recording as the truth-bearing path.

**Affected scope:** `.github/workflows/project-truth-autolog.yml`, `scripts/project_truth_local.py`, `PROJECT_TRUTH_PROTOCOL.md`

**Regression proof:** `tests/unit/test_project_truth_local_guard.py`, `Project Truth Guard on canonical main`

**Evidence:** `github-actions:Project Truth Guard run 34508421474`

**Fix:** `d66a2090bf8d73fd6d192d438a4dea745a8e86c8` — fix(project-truth): reconcile protected main guard
