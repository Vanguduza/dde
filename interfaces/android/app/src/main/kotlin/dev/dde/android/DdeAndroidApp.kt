package dev.dde.android

/**
 * Placeholder Application entry for the Compose shell (DDE-053).
 * Full APK assembly requires JDK 17 + Android SDK; Gateway client logic
 * lives in :gateway and is proven via Python reconnect tests until CI
 * grows an Android job (EDR-0029).
 */
class DdeAndroidApp
